# 의존성
/node_modules
/.pnp
.pnp.js

# 빌드 산출물
/.next/
/out/
/build
next-env.d.ts

# 로컬 데이터 (Phase 1 로컬 JSON 스토어 — 절대 커밋 금지)
/.data/

# Supabase CLI 로컬 링크 캐시 (project ref 등 로컬 상태)
/supabase/.temp/

# 환경 변수 (값 포함 파일은 절대 커밋 금지, .env.example만 공개)
.env
.env.*
!.env.example

# 로그·시스템
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# IDE
.vscode/
.idea/
.vercel

# === git push 안전 강화 (2026-05-31) ===
# 기존 /node_modules·/.next 는 루트 전용 → 중첩(ggui/**, servers/**) 까지 모두 차단
node_modules/
.next/
out/
dist/
*.tsbuildinfo
# 대용량 알파 백업(882M, 커밋 불필요)
/ggui.alpha3.bak/
# env 백업 변형(이미 .env.* 로 잡히나 명시)
.env*.bak*
# OMC 로컬 에이전트 상태 / 테스트 산출물 (제품 repo에 불필요)
.omc/
/test-results/
